# frozen_string_literal: true

source 'https://rubygems.org'

# Environment variable management
gem 'dotenv', '~> 2.8'

# Template rendering with variable substitution
gem 'mustache', '~> 1.1'

# Note: The following standard library gems don't need explicit installation:
# - json (included in Ruby standard library)
# - net/http (included in Ruby standard library)
# - uri (included in Ruby standard library)
# - securerandom (included in Ruby standard library)
